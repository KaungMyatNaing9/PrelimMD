// src/components/ReportViewer.tsx
// Displays the generated clinician-facing intake report for a completed session.
//
// TODO: Frontend - fetch report JSON from GET /report/:sessionId
// TODO: Report   - render structured summary sections (chief complaint, risk level, notes)
// TODO: Frontend - add "Download PDF" button that calls the report endpoint

export {};
