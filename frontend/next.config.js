// next.config.js
// Next.js configuration — extend here for custom headers, redirects, image domains, etc.
//
// TODO: Frontend - add API proxy rewrites so /api/* → backend URL (avoids CORS in dev)

/** @type {import('next').NextConfig} */
const nextConfig = {
  // TODO: Frontend - enable experimental app router features as needed
};

module.exports = nextConfig;
