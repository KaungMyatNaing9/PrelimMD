# requirements.txt
# Core backend dependencies — add feature-specific libraries in their own PR.
#
# TODO: AI     - add openai / anthropic SDK when implementing ai_engine.py
# TODO: Report - add reportlab or weasyprint for PDF generation

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.9   # required by FastAPI for UploadFile / form data

# ── Voice ─────────────────────────────────────────────────────────────────────
deepgram-sdk>=6.1.1
websockets<14          # deepgram-sdk uses websockets.legacy which was removed in v14+
